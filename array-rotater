def rotate_array(nums, k):
    x = k+1
    new = []
    for i in range(len(nums)):
        if i+x > len(nums):
            new.append(nums[i-x +1])
        else:
            new.append(nums[i-x +1])
    return(new)
